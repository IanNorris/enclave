# Tasks

- [ ] 1.1 `build_judge_prompt`: label responses `### Response {i}` (drop `(from {label})`)
- [ ] 1.2 `build_synthesizer_prompt`: same anonymised labelling
- [ ] 1.3 Confirm the UI trace still shows participant model names (from `participants[].model`), i.e. the human is not blinded
- [ ] 1.4 Unit test: rendered judge/synth prompts contain no model id from the participant labels
